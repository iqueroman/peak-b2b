# Ignore the tooling/docs that exist today. Anything added later
# (including archives) is tracked normally, unless it lives inside
# one of these directories.
/.agents/
/.claude/
/docs/
/AGENTS.md
/skills-lock.json
/.scratch

# Pipeline artifacts. Regenerated by `make eligible`; committing them would
# invite someone to read a stale eligible list as current.
/out/
